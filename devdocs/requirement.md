主要逻辑：
    文章以文件的方式存放于各个文件夹，文件夹是分类目录，每个分类（叶子文件夹）中有若干文章，主界面左侧目录加载文件夹树，右侧文章列表显示当前分类（叶子文件夹）中的文章文件，文章列表项包含创建日期、文件标题和文章总结，点击文章列表项，打开文章，跳转到文章页面

界面风格：
    Botanical Organic Serif风格

文件格式：
    1. Markdown，需要支持完整的Markdown文档解析，包含Latex，代码（支持各种语言）等
    2. HTML

文件夹逻辑示例：
    Folder : 0x0 - Inbox（分类目录，目录下再无目录，也为叶子目录）
        File : Note1.md
        File : Note2.html
    Folder : 0x1 - Concepts（分类目录）
        Folder : 0x0 - Math（分类目录，目录下再无目录，也为叶子目录）
            File : Note1.md
            File : Note2.html
        Folder : 0x1 - Art（分类目录，空目录）

    只有叶子目录可以存放文件

主界面：
    1. 左侧为目录，保持当前样式，目录名称左侧的箭头改成墨绿色圆点，选中的目录及所有父目录亮起浅绿色，颜色都是莫兰蒂低饱和，当点击目录时，若有子目录将展开目录，若为叶子目录则加载当前目录下的文章到右侧，空目录点击无反应，再次点击目录则收起当前目录，即便是有子目录打开，名称左侧圆点恢复墨绿，每个目录上显示当前文件夹下包含子目录一共有多少文章的数量数字，保持当前样式即可
    2. 右侧为当前目录下的文章列表，保持当前样式，列表项中包含的信息有时间，标题和总结，列表项的高度随总结字数变化，总结内容要可以完全显示，当前有一个阅读时间，去掉这个功能
    3. 默认打开主页时，右侧文章列表现在显示Posts，改成Latest，这里的显示逻辑是按照时间顺序显示所有笔记
    4. 主界面标题栏右侧的日期去掉

文章页面：
    加载逻辑：
        1. 若文件格式为Markdown，则解析并渲染Markdown文件，文章页面元素自上往下，依次是1. 目录路径 · 时间 2. 文章标题（文件名称） 3. Markdown文件，其实和当前一致，只不过当前加载的时HTML，用了CSS样式，现在的HTML的文章方式去掉，直接解析md即可，根据当前界面风格对md做样式优化即可
        2. 若文件格式为HTML则直接加载HTML文件，不需要做任何样式改变，原HTML什么样式，保留即可，两套加载逻辑不要有耦合，是分开独立的
    页面布局及功能：
        1. 左侧为文章目录大纲，贴左侧边缘显示，默认情况下根据标题数量显示长短不一的线条，长短粗细根据标题的级别不同，注意要美观，当鼠标移入则显示完整文字大纲，要有动画效果，鼠标指向的标题进行放大，上下邻近的也要有一定放大效果，类似于Mac的Dock栏的缩放效果，移出时则完整文字大纲收起变为线条，也要有动画，无论是线条模式还是完整文字大纲模式都要在页面垂直方向居中显示，而且还是以Overlay（浮在当前页面上）方式存在，不影响中间文章的布局
            a. 若文章格式为Markdown，则显示#语法的标题
            b. 若文章格式为HTML，则显示<h>标签语法的标题
        2. 中间加载的为Markdown或HTML的文章，其实是占据这个页面
        3. 右侧为文章中的引用，贴右侧边缘显示逻辑与文章目录大纲一致
        4. 在标题栏右侧（当前日期的显示位置）添加返回按钮，原来的返回按钮移除，返回按钮文字为英文
    文章引用功能说明：
        1. Markdown：自动加载Markdown中的[显示文本](链接地址)这种语法，在文章内显示文本处加上脚注，右侧文章引用显示时与左侧文章目录大纲字体颜色一致，条目内容是显示文本，有序号，序号与脚注对应，且为超链接，点击可跳转，不要有下划线等，样式与左侧文章目录大纲保持一致即可
        2. HTML： 自动加载<a href="链接地址">显示文本</a>这种语法，显示逻辑同上

manifest构建逻辑：
    manifest分类：
        1. 根目录（当前为posts文件夹）下有一个manifest文件，记录所有文章列表，包含创建时间、标题和总结
        2. 每个叶子文件夹目录下有一个manifest文件，记录当前文章列表，包含创建时间、标题和总结
    叶子文件夹文章列表：
        当用户点击左侧列表项叶子文件夹时，读取当前文件夹下的manifest文件，若没有则创建，若有则对比manifest第一行的标记，此标记代表本文件内缓存的文章数量，若此标记与当前文件夹下的html+md的数量一致，则返回文章列表，若不一致，则重新构建，并更新标记
    默认打开主界面全部文章列表：
        1. 网站打开时读取根目录（当前为posts文件夹）下的manifest文件，如果没有则创建，此时执行整个文件夹的扫描，全部构建，若有则直接返回
        2. 当叶子文件夹发现文章数量不一致导致重新构建叶子文件夹的manifest时，则同步执行对根目录manifest的增量更新
        3. 根目录文章列表manifest一定是按照时间顺序排列
    使用技术：
        因为不想每次发布文章都构建一次manifest文件，因为我可能频繁发布，所以希望带后端，直接使用Node.js做判断
    优化想法：
        以上是我现阶段的想法，能否优化？可以使用文件监听系统来监听文件的变化吗？当文件发生变化时，则自动重建manifest文件，但要避免性能问题

偏好技术：
    nginx、node.js

暂定的文章列表主要分类文件夹
Index:
0x0 - Inbox
0x1 - Concepts
0x2 - Books
0x3 - Films
0x4 - Skills
0x5 - Learning
0x6 - Ideas
0xF - Archives

---

2026年7月7日 17点35分
1. 左侧目录的·改成和文章列表项中的·统一颜色
2. 当点击非叶子节点展开目录时，目录字体及前方·改成这个颜色： #4f5e4a，当目录收起时颜色还原
3. 文章界面中的左侧的目录大纲，现在的逻辑默认状态下根据Markdown的#语法和HTML的<h>语法提取标题，根据级别不同粗细和长短不同
    现在需要在默认状态下显示更加紧凑小巧，比现在更加贴近边缘，当鼠标移入通过动画变成标题，标题采用粗体，左对齐，垂直居中，根据标题等级字号不同，但标题1字体也不能太大，主要为了页面美观，
    现在的仿Mac的Dock栏放大效果去掉，鼠标指向的文字有颜色变浅效果，点击文字进行跳转，目录收起，鼠标移出也会自动收起
    右侧引用默认也采用更加小巧紧凑的线条布局，展示逻辑跟目录大纲一致，但字号大小一致即可，也不可太大，保持界面美观
4. Markdown页面的字体整体减小一号

1. 主页左侧目录叶子节点选中时，左侧的·应该和目录文字一样的颜色
2. Markdown页面的标题，我现在分别率下的字体大小我感觉30px大小看着比较舒服
3. 文章页面的左侧标题大纲默认状态下上下间距再更紧凑些就可以了，线条可以再粗一点点，鼠标移入后不需要显示线条，这些字从小放大出来，放大出来的字体再大一些，不同标题大小的字体区分明显一些，
    做确认一边鼠标移入后的逻辑，线条变成非常小的字体，跟线条高度一样大小的字体，然后缩放成完整的标题，因为标题字体比较大，所以应该是缩放出现动画带一些曲线，比如紧凑的线条最上方的，放大后可能变到更高的位置，这个缩放高度变化要考虑到，不是直上直下的缩放，而是文字像线条一样流动过去
    鼠标移出后直接原地透明度淡出即可
    
1. 你说的Markdown 文章页顶部标题固定为 30px。还是按照原来的缩放逻辑，不是固定的，只不过我现在1920*1080下30px比较舒服，之前是font-size: clamp(30px, 3.6vw, 44px);
2. 文章左侧标题大纲默认状态下的线条应该是居左对齐，展开后的列表字体上边缘被裁切了，字上下间距再大一点，靠左边缘间距再大一点，点击标题跳转后，变成了很短的线，并没有完全消失，列表出现动画取消放大后回弹的效果，直接放大即可，鼠标移出后线条出现动画好像有点延时，感觉是文字列表消失后再开始线条出现动画，如果是这样，他们之间的动画应该有交错
3. 右侧引用动画方向反了，又该是从右往左
4. 做的这些修改，如果右边引用也存在，一起修改

文章页面左侧引用的动画效果修改
1. 移出后文字列表透明淡出，横线列表淡入即可，不需要现在的缩放动画
2. 文字出现的动画更快一些
3. 有时候文字列表完全显示后，h2级别的标题会从上到下移动一下
4. 每一级标题有些许向右错位，表示属于不同级别的标题
5. 如果列表过长，显示出界面，则可以通过鼠标滚轮上下滚动显示

---

2026年7月8日 9点25分
1. 文章页面大纲列表当文章大纲过长时现在显示滚动条，不要显示滚动条
2. 现在鼠标移出后文字先消失，然后横线大纲有一个缩放回去的动画，去掉这个缩放动画，让文字渐隐消失的同时横线标题就渐隐出现，不要缩放动画

1. 文章页面大纲列表当文章大纲横线的hover区域改成只比横线区域宽一些，现在触发范围太大了
2. 现在把点击文字大纲后收起文字大纲列表的功能去掉，现在点击直接跳转，不会收起列表

1. 左侧文章大纲hover 命中区改成现在的两倍，大概两个横线区域的大小即可，但注意是横线区域向右平移一个区域，不是以当前hover区域为中心向两边扩大
2. 检查右侧引用区域，将右侧出现消失、列表文字（这里的引用没有层级关系）显示和hover等的各种逻辑都保持一致，字号调大一号，注意字体是否显示完全，还要注意区分左右逻辑，如果有动画，要注意左右关系

Just trying to grasp things easier with AI.

我现在有这样的需求，当我提交文章到这个Github仓库后，自动触发我的服务器上下拉这个仓库，这时候文件更新，触发自动更新manifest，我的网页刷新就能看到新的文章，可以做到吗

我现在需要将这个网站部署到我的服务器上，我的服务器有nginx和node.js，我需要部署到55555端口，给我一份部署文档