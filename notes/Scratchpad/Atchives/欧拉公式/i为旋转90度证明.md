“$i \times i = -1$ 代表两次相同的 90° 旋转”是最直观的几何直觉，但在数学中，这其实更像是一种**动机（Motivation）**或**定义（Definition）**。

---

### 1. 向量点积与长度证明法（几何代数法）

这种方法不需要预先假设旋转，只利用最基础的向量性质来证明。

在二维平面上，复数 $z = a + bi$ 对应向量 $\mathbf{v} = (a, b)$。
当它乘以 $i$ 时：


$$z \cdot i = (a + bi)i = -b + ai$$


对应新向量 $\mathbf{v}' = (-b, a)$。

我们需要证明两件事：**角度改变了 90°** 且 **长度没有改变**。

* **验证角度（垂直）**：
计算两个向量的点积（Dot Product）：

$$\mathbf{v} \cdot \mathbf{v}' = a \cdot (-b) + b \cdot a = -ab + ab = 0$$



因为点积为 0，根据线性代数，这两个向量**严格垂直**（夹角为 90°）。
* **验证长度（无拉伸）**：

$$\Vert{}\mathbf{v}\Vert{} = \sqrt{a^2 + b^2}, \quad \Vert{}\mathbf{v}'\Vert{} = \sqrt{(-b)^2 + a^2} = \sqrt{a^2 + b^2}$$



长度完全相等，说明点**没有被放大或缩小**。
* **验证旋转方向**：
取最简单的基底向量 $(1, 0)$，乘以 $i$ 后变成 $(0, 1)$。在标准直角坐标系中，从 $(1,0)$ 到 $(0,1)$ 正好是**逆时针旋转 90°**。

**结论**：在不改变长度的前提下垂直转向，必然是 90° 旋转。

---

### 2. 矩阵表示法（线性代数法）

在线性代数中，平面上的所有“线性变换”（包括旋转、缩放、剪切）都可以用一个 $2 \times 2$ 的矩阵来表示。

复数乘法本质上是一种线性变换。我们把复数 $a + bi$ 写成矩阵形式：


$$a + bi \iff \begin{pmatrix} a & -b \\ b & a \end{pmatrix}$$

那么，虚数单位 $i$（即 $0 + 1i$）对应的变换矩阵 $M_i$ 就是：


$$M_i = \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$$

另一方面，根据二维空间中的**旋转矩阵公式**，一个逆时针旋转 $\theta$ 角的矩阵为：


$$R(\theta) = \begin{pmatrix} \cos\theta & -\sin\theta \\ \sin\theta & \cos\theta \end{pmatrix}$$

我们将 $\theta = 90^\circ$（即 $\frac{\pi}{2}$）代入旋转矩阵：


$$R(90^\circ) = \begin{pmatrix} \cos 90^\circ & -\sin 90^\circ \\ \sin 90^\circ & \cos 90^\circ \end{pmatrix} = \begin{pmatrix} 0 & -1 \\ 1 & 0 \end{pmatrix}$$

可以看到：


$$M_i = R(90^\circ)$$

**结论**：乘以 $i$ 的变换矩阵与逆时针旋转 90° 的旋转矩阵**完全恒等**。

---

### 3. 三角形式与三角恒等式证明法（代数严密法）

任何复数都可以用极坐标的形式（三角形式）表达为：


$$z = r(\cos\alpha + i\sin\alpha)$$


其中 $r$ 是模长，$\alpha$ 是与实轴的夹角。

虚数单位 $i$ 的模长是 $1$，夹角是 $90^\circ$，所以：


$$i = 1 \cdot (\cos 90^\circ + i\sin 90^\circ)$$

现在，我们将 $z$ 乘以 $i$，根据复数乘法和三角和角公式：


$$z \times i = r(\cos\alpha + i\sin\alpha) \times (\cos 90^\circ + i\sin 90^\circ)$$

展开并利用三角恒等式：

* 相对实部：$\cos\alpha\cos 90^\circ - \sin\alpha\sin 90^\circ = \cos(\alpha + 90^\circ)$
* 相对虚部：$\sin\alpha\cos 90^\circ + \cos\alpha\sin 90^\circ = \sin(\alpha + 90^\circ)$

因此：


$$z \times i = r \big[ \cos(\alpha + 90^\circ) + i\sin(\alpha + 90^\circ) \big]$$

**结论**：乘以 $i$ 之后，模长 $r$ 保持不变，但幅角从 $\alpha$ 变成了 $\alpha + 90^\circ$。这在代数上严格证明了**角度增加了 90°**。

---

> **一句话总结：**
> **不管是看点积为 0（几何）、比对旋转矩阵（线代），还是推导三角和角公式（代数），都独立且严谨地证明了乘以 $i$ 就是逆时针旋转 90 度。**